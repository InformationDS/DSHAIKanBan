# 交接上下文（AIKanBan for DSH）

- 工作区：D:\BaiduSyncdisk\dsh-plugin
- 生成时间：2026-08-17T15:43:59.533Z

## 项目记忆（V1，确认于 2026-08-17T15:43:21.793Z）
### 项目目的与范围
dsh-plugin 工作区：为 DSH 构建 AIKanBan 风格的 Trello 式项目看板插件——任务分配给对话完成、跨会话记忆交接；同时以 PRD.md（Codex 原版）与 DSH-PRD.md（v2 契约）为需求事实源，用插件自管本插件开发。
### 共享约束与工作约定
1) 记忆建议必须经用户在看板 diff 审核后确认才成为正式记忆，未确认内容不得进入交接上下文；2) 工作项状态由用户最终决定，agent 只在用户明确要求时直接修改，或在建议 fieldSuggestions 中提出随确认生效；3) 同一范围（任务=同一工作项，项目=全局）同时只允许一条活动建议；4) 数据持久化在工作区根目录 .dsh-kanban.json；5) 动态插件代码可重建：Host 源码 .host-v2.js、Client 源码 .client-check.js，均经 node --check 校验；6) 工具定义用 harness.defineTool({parameters, output:{schema,render}})，写入必须传 sandboxPolicy.resolve({session}) 的策略，返回值的 undefined 字段必须归一为 null。
### 已确认的项目级决策
1) 产品形态：DSH 会话头部与「聊天」并列的「看板」全宽视图 Tab（conversation.view 槽位，id aikanban-board），不用浮层挂件；2) 看板交互：5 列状态流 + 卡片拖拽流转 + 按钮流转并存；3) 对话为一等对象：工作项详情含参与对话时间线，每条对话有独立详情页（参与任务/上下文版本状态/产出的建议与记忆版本）；4) 任务不分配给「人」而分配给「对话」，一个任务可由多条对话串行接力或并行探索。
### 跨工作项上下文与依赖
当前只有一个工作项（wi-msxbo1g2-rt37fz：看板插件 v1 开发），后续 UI 迭代、交接验证等可拆分为独立工作项跟踪；对话（DSH 会话）通过起草/提交/确认建议、手动编辑记忆、生成交接上下文时自动登记为参与对话。
### 项目级未决问题与风险
1) 跨会话交接尚未在真实的新 DSH 会话中验证；2) 建议过期场景、归档/删除流程尚未实际操作验证；3) 进程重启会清空动态插件注册，需重建（数据不受影响，已验证）。

## 工作项：AIKanBan for DSH 看板插件 v1 开发
- 状态：进行中
- 目标：为 DSH 实现 AIKanBan 风格看板插件 v1：单项目（当前工作区）持久化数据、工作项 5 状态管理、任务/项目记忆版本链、agent 起草建议→看板 diff 审核→用户确认闭环、交接上下文生成与读取工具、看板 UI 嵌入 Web GUI，并用插件自管本插件开发验证闭环。
- 当前进度：v2 全宽看板已重构并验收通过（看板 Tab、拖拽流转、对话时间线、对话详情页、审核队列）；任务记忆 V1、项目记忆 V1 均已确认。
- 下一步：1) 新会话验证跨会话交接（本文件即交接物）；2) 在对话时间线上确认出现第二段对话；3) 后续可起草任务记忆 V2 建议记录 v2 验收结果。

## 任务记忆（V1，确认于 2026-08-17T15:41:06.414Z）
### 任务目标与范围
为 DSH 实现 AIKanBan 风格看板插件：单项目 = 当前工作区；工作项 5 状态管理；任务记忆（6 分区）/项目记忆（5 分区）不可变版本链；agent 起草建议 → 看板 diff 审核 → 用户确认闭环；交接上下文生成与读取工具；全宽看板 UI；用插件自管本插件开发验证闭环。
### 已确认的决策与约束
1) 需求对齐五项均取推荐方案。2) 数据持久化为工作区根目录单文件 .dsh-kanban.json。3) 同一范围同时只允许一条活动建议。4) 工作项字段由用户最终决定。5) 动态工具必须用 harness.defineTool({schema, render}) + registerTool；写入必须传 sandboxPolicy.resolve({session}) 解析的策略。6) 工作区解析：live 会话唯一 cwd → sessionPersistence header.cwd → workspaceRegistry 单例 → policy root → 文件推导。
### 已完成工作与当前进展
v2 重构完成：全宽看板视图 Tab（conversation.view）、5 列拖拽流转、工作项详情（对话时间线）、对话详情页（参与任务/上下文版本状态/产出）、审核队列、项目记忆页、交接页；undefined 归一化修复；经用户浏览器验收通过；任务记忆 V1 与项目记忆 V1 已确认。
### 关键产物与验证结果
工作区源码：.host-v2.js（Host，node --check 通过）、.client-check.js（Client，node --check 通过）、DSH-PRD.md（v2 契约）、REBUILD.md（新会话重建指引）；数据 .dsh-kanban.json。
### 未解决问题、阻塞与风险
1) 跨会话交接未验证（本次新会话即验证）。2) 建议过期、归档/删除未实操。3) 进程重启清空动态插件注册（需重建，已演练两次）。
### 下一步
1) 新会话按 REBUILD.md 重建插件并批准。2) 用 kanban_view 确认工作项与两条 V1。3) 看板对话时间线应出现第二段对话（新会话）。

## 本次补充说明
v2 看板插件验收通过：任务记忆 V1、项目记忆 V1 已确认。新会话任务：用看板工具继续工作项（对话时间线应出现第二段对话），并验证交接闭环。
